# LEDGER

Findings live and die here, nowhere else. Reports are snapshots; this is the state.
**No row is ever deleted.** A closed finding stays closed in place.

**Status:** `open` · `in-progress` · `fixed-unverified` · `verified-fixed` · `risk-accepted`
**`verified-fixed` requires a fresh session re-running the original reproduction.** The fixer never
closes their own finding. **`risk-accepted` is the owner's alone**, written by him, with rationale and
an expiry date — never set on his behalf.

Severity → due date: Critical 3 days · High 2 weeks · Medium 6 weeks · Low next milestone.

| ID | Sev | Class | Finding | Found | Due | Status |
|---|---|---|---|---|---|---|
| L-001 | Low | correctness | **v0 applies interval fuzz after the breadth cap**, so a capped 30-day interval can reach 32 — the gate is ~5% leakier than v0's README states. v1 fuzzes first and caps second, making `interval <= cap` an actual invariant. Divergence from v0 is deliberate and is inside the simulation's tolerance. | 2026-08-04 | — | `verified-fixed` — asserted in `scheduler.test.ts` ("never exceeds its cap even at the extremes of fuzz") across seven fuzz draws, and by the 60-day simulation's per-review invariant check |
| L-002 | High | content | **Numeric distractors bracket the true value.** The correct answer is a middle value in 91.4% of 373 all-numeric forms (chance 50%). "Pick a middle number" scores ~91% with no knowledge. This is R1 and it is upstream of the whole readiness model. | 2026-08-04 | 2026-08-18 | `fixed-unverified` — generated distractors (D-014/D-021) bring the **on-screen** rate to **52.7%** against a chance floor of 50%, with rank distribution 25.0/25.0/24.9/25.1 over 8,000 draws. Ratcheted at 0.53. **Needs a fresh session to re-run the original measurement and close it — the fixer does not close their own finding** |
| L-003 | Medium | content | **Correct answer is the longest option in 40.2%** of the 749 forms with a unique longest (chance 25%). Survives option shuffling, because it is a property of the option set rather than its order. | 2026-08-04 | 2026-09-15 | `fixed-unverified` — **0.403 -> 0.299, target reached** by the chapter-3 option audit, which rewrote 87 distractors and length-balanced the sets it touched. Ceiling lowered 0.315 -> 0.30 in the same commit. Needs a fresh session to re-derive the original measurement |
| L-004 | Medium | a11y | **v0's palette is inherited unaudited.** The amber "check the book" pairing (`--ref-amber-ink` #8a5a00 on a 20%-amber fill) is the most likely WCAG 2.2 AA contrast failure. "Keep v0's palette" (D-018) means keep it *and fix it*. | 2026-08-04 | 2026-09-15 | `open` — no component uses it yet; must be resolved before the first UI milestone |
| L-005 | Medium | content | **12 facts flagged for checking against the printed handbook.** The examinable answer is the book's even where the book is now wrong, so both drift and over-correction are failure modes. Blocks the launch gate. | 2026-08-04 | 2026-09-15 | `fixed-unverified` — **11 of 12 resolved** against the full 3rd-edition text the owner supplied (D-023). Eight confirmed correct (f212, f255, f284, f286, f354, f355, f372, f386), three corrected (see L-015). All eleven now carry a `source` citation and are declared in `divergences.ts`. Ratchet lowered 12 → 1 |
| L-015 | **High** | content | **Three facts were wrong or stale.** f387: small claims limit for Scotland and Northern Ireland was **£3,000**, handbook says **£5,000** — a straightforwardly wrong answer that spaced repetition would have drilled to permanence. f368: Commonwealth membership was **54**, corrected to **56** (the source says 54 in prose and 56 above its member list, which includes Gabon and Togo, admitted 2022). f253: UK population was **about 62 million**, the handbook's table now gives **67.6 million** estimated for 2022. | 2026-08-04 | 2026-08-04 | `fixed-unverified` — corrected, sourced and declared. **Needs a fresh session to re-check each against the handbook independently; the fixer does not close their own finding** |
| L-016 | Medium | content | **f213 cannot be resolved from this source.** The KoLL age exemption (under 18 or 65 and over) is a Home Office rule and does not appear in the handbook text, so the supplied edition cannot settle it. The last remaining amber fact and the only thing standing between L-005 and closure. | 2026-08-04 | 2026-09-15 | `fixed-unverified` — **resolved by retiring f213 on 10 Aug 2026**, on the owner's instruction, rather than by finding another source. A fact this project's source cannot settle either way is not safely examinable, and holding it amber indefinitely was the worse option. Takes `unresolvedVerifyFlags` to 0 and clears launch-gate item 2. Needs a fresh session to confirm the retirement landed and the flag is genuinely zero |
| L-017 | Medium | content | **Coverage gaps measured against the handbook.** Thin against the handbook's own weighting: the EU/EEC (**1** fact against a full section), the modern constitutional monarchy (2, while 30 mention medieval kings), the Industrial Revolution (4, all tagged *Inventors*), local government, civil vs criminal law and the civil service (1 each), architecture (2). *(Corrected: first reported as **zero** EU facts — see L-018.)* | 2026-08-04 | 2026-09-15 | `fixed-unverified` — 33 facts added under D-024, sourced against the handbook. Deck 410 → **443** facts, 1,228 → **1,327** forms. Still open for the thinner remainder: traditional food, media, the interwar period, national flags |
| L-018 | Medium | process | **The gap analysis over-claimed, and the deck's own checks caught it rather than review.** The probe searched canonical questions and answers but not form text, so it reported zero EU facts when f188 covers the UK joining the EEC, and two facts short elsewhere. Two drafted additions duplicated f188 and f331 and were rejected by the duplicate-canonical-question and shared-form checks on first run. | 2026-08-04 | — | `verified-fixed` — probe corrected to search all form text and re-run; the corrected figures are in L-017. The finding worth keeping: a probe over a subset of the data reads exactly like a probe over all of it, and nothing in the output says which it was |
| L-006 | Low | content | **f193 and f352 serve an identical question form.** Form 1 of each is verbatim "The Scottish Parliament and the Welsh Assembly first met in which year?" with the same answer, 1999. The facts themselves differ — f193 is "In which year did the Scottish Parliament and Welsh Assembly first meet?" (ch. 3), f352 is "In which year did the devolved administrations first receive their powers?" (ch. 5) — so this is a shared sentence, not a duplicated fact. It matters because one memorised sentence earns breadth credit on two separate facts, which is the specific thing the breadth gate exists to prevent. *(Corrected 2026-08-04: first logged as "the same fact, identical canonical question". The measurement said otherwise and the measurement was right.)* | 2026-08-04 | 2026-09-15 | `open` — fix is to reword one of the two forms, which needs no index remap. Caught by neither original check; `sharedFormsAcrossFacts` added and ratcheted at 1 |
| L-007 | Medium | content | **f205 and f206 share the stem "Which of these is one of the fundamental principles of British life"** with different correct answers, and both are served as free recall (`mcqOnly = false`). Several answers are legitimately correct, so an honest answer gets marked wrong. | 2026-08-04 | 2026-09-15 | `open` — cheapest fix is to mark both `mcqOnly`, which costs two recall forms |
| L-008 | Low | content | **Six facts have only one recall-usable form** (f017, f218, f222, f223, f357, f367), so in Cards-only use their breadth can never exceed 1 and they stay pinned at the 6-day cap for ever. 43 more have only two and are pinned at 30 days. From the inside this reads as "I keep failing these". | 2026-08-04 | 2026-09-15 | `open` — reproduced exactly by the recall-only simulation, which asserts the pinned set is precisely those six |
| L-009 | Low | ops | **The v0-deploy fence is partly conventional.** CI and the pre-push hook both refuse any *modification* to v0's files, but neither can prevent a `vercel --prod` aimed at v0's project. Mitigated by v1 living in its own directory with its own Vercel project (D-020) and by deny-rules on raw `vercel --prod`. | 2026-08-04 | 2026-09-15 | `open` — residual risk stated rather than solved; revisit when the v1 Vercel project is created |
| L-010 | Low | tooling | **The simulation's learner model conflated credit with exposure.** Recall probability keyed off `state.ok[form] === 0`, which a lapse resets — so every lapse silently returned the modelled learner to never-seen, and `pickForm` then served that form preferentially. Produced ~1,200 phantom lapses and a review curve that never settled, on a correct scheduler. | 2026-08-04 | — | `verified-fixed` — exposure now tracked by the harness independently of scheduler state; week-6 load fell from 68/day to 41/day and proven-all-forms rose from 395 to 397, both consistent with v0's documented run |

| L-011 | Low | content | **56 all-numeric forms cannot carry a generation rule** and keep their authored options, with a middle-value rate of about 68% among themselves. Three shapes, all legitimate rejections: full-sentence variants ("It lasted 116 years" vs "It lasted exactly 100 years"), date ranges ("1756–1763"), and mixed qualifiers ("More than 400 million" vs "Under 200 million"). They are the entire residual between 52.7% and chance. | 2026-08-04 | 2026-09-15 | `open` — closing this is authoring, not engineering: each needs its options rewritten to share one shape. Date ranges (f125, f143 and similar) are the one group a second rule type could handle |
| L-012 | Low | content | **`f387[2]` cannot place its answer at every rank.** The £3,000 Scottish/NI small-claims limit has granularity £1,000, so only two candidates fit below it and the answer is never the largest option on screen. A narrow residual tell on one form. | 2026-08-04 | 2026-09-15 | `fixed-unverified` — measures 0 over both `DECK` and `ACTIVE` as of 10 Aug 2026, so the ceiling was lowered 1 → 0 and the ratchet became an assertion: any form that cannot place its answer at all four ranks now fails the build. Needs a fresh session to re-derive the original measurement |
| L-013 | Low | ops | **The Vercel project serves v0, not v1.** Root Directory is `.`, so `lituk-drill.vercel.app` returns the repo-root `index.html`. Raised as High on the assumption — taken from the kickoff document — that another v0 was already deployed and carrying the owner's schedule, which would have made this a second copy at a new origin with empty storage. **That assumption was false (L-014), and the resolution inverts: this IS the v0 deployment, and it works.** Verified live: `index.html` 28,439 bytes and `facts.js` 232,747 bytes, both exact matches for the local files, both 200. | 2026-08-04 | — | `verified-fixed` — resolved by keeping it, not by changing it. Root Directory stays `.`; v1 gets its own project (D-022). Stable by construction: R-1 and the `v0 is untouched` CI job forbid any change to the files it serves |
| L-014 | **High** | process | **The BRIEF rests on a premise that was never true.** §B states "a working v0 is already deployed and in daily use… it carries him to 25 September on its own", taken verbatim from the kickoff input and never checked. There was no deployment and no accumulated schedule. Everything built on it — the neutralised deadline, "if v1 is not ready by 20 September he sits the test on v0 and loses nothing", the whole justification for a 6-week appetite ignoring the exam date — was resting on nothing. | 2026-08-04 | 2026-08-04 | `fixed-unverified` — v0 is now genuinely deployed and verified (L-013), so the arrangement holds **from today, conditional on daily use actually starting**. Corrected in the BRIEF by D-022. Tripwire: `/jorge-drift-check` must re-read §B's premise as a claim to verify, not a given |

| L-021 | **High** | correctness | **The four options re-shuffled mid-click, so the verdict was reported against a layout that had already gone.** The shuffle seed included `remaining` — a per-section count derived from the review-event log. Answering appended an event, the count moved, the memo recomputed, and the options re-ordered. `chosen` then indexed the old arrangement while `correctIndex` came from the new one, so a right answer could be marked wrong and a wrong one right. The *recorded grade* was correct throughout (the click handler closed over the pre-answer arrangement), so the schedule was never corrupted — but the screen lied, which for a learning tool is the worse half. | 2026-08-04 | 2026-08-04 | `fixed-unverified` — seed now takes a `cardNonce` that advances only with the card. Three regression tests across the affected sections; all three fail against the old code |

| L-022 | Medium | process | **Four ids were spent without an entry ever being written.** `D-026` and `L-020` were skipped outright — HANDOFF advertised it as next, and the next entry written was D-027. `D-029` is named in a commit subject and a CHANGELOG heading (the 443 → 528 expansion) but has no DECISIONS entry, so a deliberate amendment to a BRIEF non-goal exists only as a changelog paragraph. `L-019` is cited by RULES R-11 and by the postmortem as one of the two bugs that produced the rule, and has no row here. The rule sets say plainly that a numbered entry vanishing without trace is how a rule set stops being trustworthy; this is that, three times, and it was found by reading rather than by any check. | 2026-08-04 | 2026-09-15 | `open` — D-029 and L-019 need writing from the CHANGELOG and the R-11 postmortem, which still hold the reasoning. D-026 and L-020 were never used and should be recorded as retired-unissued rather than back-filled. Tripwire: a docs-consistency test that fails on an id referenced anywhere in the repo with no entry under it — the same check `/jorge-drift-check` would want |

| L-023 | **High** | content | **Four facts ask for an answer the handbook does not contain, and three explanations teach material that is not in it.** Found by the new vocabulary check on its first run, then each one grepped against the handbook text by hand rather than taken from the report. **Answers not in the handbook:** f046 "In which year did the Hundred Years War begin? → 1337" — the handbook gives no start year, only that the war "lasted 116 years" and that the English left France in the 1450s. f059 "In which year were Jews expelled from England? → 1290" — no expulsion is mentioned at all; the nearest text is that the first Jews to come to Britain *since the Middle Ages* settled in London in 1656. f391 and f392, the two FGM dates (1985 and 2003) — the handbook says only that FGM is illegal and that practising it or taking a girl abroad for it is an offence, with **no dates whatsoever**. **Also, found once the check learned to read `AD 43`/`55 BC` rather than only four digits:** f015 "In which year was Hadrian's Wall begun? → AD 122" — the handbook says only that Hadrian built a wall in the north to keep out the Picts, and gives no date; and f023 "In which year did St Augustine arrive? → AD 597" — Augustine is named three times, 597 appears zero. That makes **six** facts asking for an answer the book does not hold. **Explanations only:** f096 invents "the Protectorate" and 1653 (neither appears); f153 introduces 1919; f499 explains decimalisation in 1971 and 240 old pence, and the word "decimal" appears nowhere in the handbook. | 2026-08-04 | 2026-08-18 | `open` — a fact the handbook cannot answer is not examinable, so this is wasted drilling rather than a wrong answer, which is why it is High and not Critical. Resolve in the sourcing pass: flag `verify` and correct or retire each. Ratcheted meanwhile at 6 distinct off-source years in `baseline.ts`, verified by adding a fake year and watching the build fail |
| L-024 | Medium | measurement | **Teaching a cluster inside one card's explanation is a second way to inflate the readiness figure.** A reader who meets fact B inside fact A's panel may later answer B's own card by recognition rather than recall. That is the same class of inflation as L-002 — the option-shape tell — arriving through a different door, and it will be invisible in the same way: performance rises, knowledge does not. Raised from the retention research before the rewrite rather than after it. | 2026-08-04 | 2026-09-15 | `open` — mitigated by the cluster conditions in `docs/EXPLANATIONS.md` (a panel may only name a fact that has its own card, and only to draw a contrast), which bound it but do not remove it. Must be accounted for when S4 is built: the exam-format number is the one exposed |

| L-025 | **High** | content | **The owner's religion and population figures disagree with the handbook text this project holds.** He supplied 2011 census figures — 59% Christian, 4.8% Muslim, 1.5% Hindu, 0.8% Sikh, Jewish/Buddhist under 0.5%, 25% no religion — and a 2021 population of 67 million. The extracted handbook reports the **2009 Citizenship Survey**: 70% / 4% / 2% / 1%, both under 0.5%, 2% other, **21%** no religion; and a population table whose last row is **2022, estimated 67.6 million**, with no 2021 row. Two of his five supplied facts DO match the text (nearly 10% with a parent or grandparent born outside the UK; England 84%), so this is a partial divergence rather than a different book. | 2026-08-04 | 2026-08-18 | `verified-fixed` — the owner settled it the same day: his 2026 edition is the authority and uses the 2011 census, and the two texts are otherwise identical apart from an enumerated list of deltas. f534/f535/f536 corrected to 59% / 4.8% / 25%, `verify` flags removed, and f539/f540/f541 added for Hindu, Sikh and the 2021 population. Recorded as D-031 |
| L-026 | Medium | content | **The 399 inherited facts were checked mechanically and 53 could not be corroborated; 16 of those have an answer that appears nowhere in the handbook in any wording.** Confirmed by hand, one grep each: `f029` Wessex (0 hits), `f036` 1086 (0), `f059` 1290 (0), `f071` "annul" (0), `f105` 1687 (0), `f125` 1756 (0), **`f148` Charles Darwin and On the Origin of Species (0 each)**, `f228` Jersey and Guernsey (0 each), `f252` Cornish (0), `f258` Supreme Governor (0), `f315` Orwell and Nineteen Eighty-Four (0 each), `f333` neutrality (0). The extract was tested for completeness before this was believed — 97 of 98 pages, and Newton, Shakespeare, Turing, Brunel, Nightingale, Beveridge and Pankhurst are all present — so it is not an abridgement artefact. | 2026-08-04 | 2026-09-15 | `fixed-unverified` — the owner answered all 53 against his own copy. **Sixteen retired**, each with its reason recorded on the fact: f023 f029 f036 f039 f046 f059 f061 f070 f071 f105 f125 f148 f228 f252 f258 f315. Thirty-seven confirmed and sourced. Retired rather than deleted, because ids are the handle the event log points at (R-4) — they keep their place in `DECK` and drop out of `ACTIVE`. Needs a fresh session to re-check a sample; the fixer does not close their own finding |
| L-027 | Low | process | **The first review spreadsheet asked the owner to check 399 facts, most of which were fine.** He said so: *"most if not all of the facts are in the book. Why are you asking me to check them?"* He was right. "Unsourced" meant the `source` field was empty, not that the fact was suspect, and 399 rows of mostly-correct material is a request that will not be completed and should not have been made. The work was mechanisable and had not been mechanised. | 2026-08-04 | — | `verified-fixed` — replaced by a checker that corroborated 346 of 399 against the handbook text, leaving 53. The lesson worth keeping: before asking a human to check a list, check how much of the list a machine can settle. The base rate that justified the ask (3 of 12 previously-checked facts were wrong) came from a *selected* sample — those twelve were flagged because they looked doubtful — so it was an upper bound being used as an estimate |

| L-028 | **High** | content | **The deck says Stonehenge is Bronze Age; the handbook reads as Stone Age.** Found by the adversarial pass over the explanation rewrite, then checked by hand: the handbook lists Stonehenge among the houses, tombs and monuments built by the first farmers *about 6,000 years ago*, and the next sentence begins "Other Stone Age sites have also survived" — which makes Stonehenge one of them. The Bronze Age paragraph never mentions it. f006's answer is "The Bronze Age". | 2026-08-05 | 2026-08-19 | `fixed-unverified` — **resolved by retiring f006 on 10 Aug 2026**, on the owner's instruction, rather than by flipping the answer. The handbook never assigns Stonehenge to an age outright, so *both* answers were unsafe and picking one would have been the over-correction R3 warns about. Needs a fresh session to confirm. This is R3 exactly, and it was caught by a reviewer arguing with a writer rather than by any check |
| L-029 | Medium | process | **The rewrite invented dates, at scale, despite an explicit instruction not to.** Fourteen agents rewrote 503 explanations against a standard whose first hard rule is "only people, dates and facts that appear in the handbook". They produced 1536, 1541, 1558, 1587, 1611, 1926 and others, none of which the handbook contains. The adversarial pass caught them and so did the merge guard, independently. | 2026-08-05 | — | `verified-fixed` — the merge script refuses any explanation naming a year that is neither in the handbook nor in the fact's own answer, so an invented date cannot reach the deck however confidently it is written. The lesson worth keeping: an instruction in a prompt is not a control. The only thing that actually stopped these was a check that ran on the way in |
| L-031 | Medium | content | **The year check has never been run over fact ANSWERS, only over explanations — and six facts were asking for a year the handbook does not contain.** f073 (1536), f074 (1541), f078 (1558), f086 (1587), f091 (1611), f163 (1926). All six carried `source: corroborated against the handbook text` from the mechanical pass over the 399 inherited facts, which matched on **topic** rather than on the answer: the book discusses the King James Bible, so the fact corroborated, and the answer is a year the book never gives. The vocabulary check could not see them either, because it reads explanations and deliberately exempts a year the fact's own answer already asserts (documented in `vocabulary.ts`; sound under D-031 only when the answer has itself been checked). Running the same year test over every drilled answer flags **two more that are live**: `f015` "In which year was Hadrian's Wall begun? → AD 122" — already named in L-023 as unanswerable, retired never happened, and all three of its phrasings turn on the date; and `f194`, whose form 2 asks "John Logie Baird demonstrated his first working television in which year? → 1924" and whose canonical stem embeds 1924 — the exact Baird defect the vocabulary check was written for, still live in the *questions* because the check only reads explanations. Three further hits are settled rather than defects (f098 1651, f101 1666, f391 1985 — enumerated D-031 deltas the owner confirmed against his own copy) and one is a false positive (f282, the 1500 metres). | 2026-08-05 | 2026-09-16 | `fixed-unverified` for the six — retired with reasons, deck 543 → 537. **f015 and f194 retired on 10 Aug 2026** on the owner's instruction, taking the deck to 533; retiring f194 costs Baird his only card and that coverage gap is carried into the S10 pass. **`open` for the mechanism:** the probe that found them is a throwaway script rather than a check. The tripwire this wants is the year test extended over `fact.answer` and every `answers.correct`, ratcheted like `explanationYearsOffSource`. Needs a fresh session to re-check the six; the fixer does not close their own finding |
| L-030 | Low | tooling | **Ten years in the population table were missing from the handbook vocabulary.** The PDF extracts the table with no space between column and value — `1600Just over 4 million` — and `\b` finds no boundary between `0` and `J` because both are word characters. Any explanation citing one read as invented. | 2026-08-05 | — | `verified-fixed` — both sides of the check now bound years by digits rather than word boundaries. Found because a hand-written explanation of my own was rejected by my own check, which is the cheapest way to find out a check is wrong |

| L-032 | Medium | measurement | **The deck ratchets measured a deck nobody drills, and one of them had eight points of slack.** `analyseDeck` was passed `DECK` — every id, retired included — by both `deck-report.ts` and the ratchet tests. A retired fact is never served, and facts are retired *for being bad*, so the retired set was flattering every rate it entered: longest-option 0.3124 against 0.3092 over `ACTIVE`, on-screen numeric 0.5277 against 0.5249. Separately and worse, `longestOptionCorrectRate` sat at a ceiling of **0.39** while the deck measured **0.312** — two hundred forms could have regressed without failing a build. A ratchet whose gap to the actual figure is never watched silently stops being a ratchet. | 2026-08-10 | 2026-09-21 | `fixed-unverified` — quality measurements re-pointed at `ACTIVE` in `deck.test.ts` and `numeric.test.ts`; id-space contracts (contiguity, uniqueness, orphaned explanations) deliberately left on `DECK`. Ceilings re-derived: longest-option 0.39 → 0.315, numeric middle 0.53 → 0.527, `restrictedRankForms` 1 → 0, `unresolvedVerifyFlags` 1 → 0. **The tripwire this wants and does not have: a check that fails when any ceiling sits more than a stated margin above its actual figure.** Found by reading, not by any check |
| L-033 | **High** | content | **Nothing in the build has ever looked at what a distractor says.** There are statistical checks over option *shape* — length rank, numeric rank, answer position — and none at all over option *content*. Two measurements taken while scoping, neither previously run: **12 forms offer as a wrong answer a string that another form of the same fact marks correct** — `f422[0]` "William, Prince of Wales", `f451[0]` "That Was The Week That Was", `f451[2]` "Spitting Image", `f454[0]` "Gertrude Jekyll", `f454[2]` "Capability Brown", `f473[2]`, `f500[2]` "£2", `f511[0]` "12 noon" among them. Each is the deck teaching that a true thing is false, and spaced repetition installs that as reliably as it installs the answer. And **618 distractor strings repeat across forms of the same fact**, shrinking the effective option pool so the same wrong answers recur — R2's memorisable surface living inside the mechanism built to prevent it. Neither is caught by anything. | 2026-08-10 | 2026-08-24 | `open` — the subject of the option-and-form audit. Some of the 12 may be legitimate (two forms can ask genuinely different questions), so each needs reading rather than a bulk fix. Wants a mechanical check plus a ratchet in both directions |

| L-034 | Medium | a11y | **`--c-text-muted` on `--c-surface` measures 3.50:1 in light mode**, below the WCAG 2.2 AA minimum of 4.5:1 for body text, and it is not large text — it is used at `--text-xs` and `--text-sm`. Live today on the chronology's header count, every era blurb, and the ＋/− expand affordance; `--c-text-muted` on `--c-fill` is worse at **3.12:1**. Dark mode passes at 4.85:1, so this is light-mode only. Measured, not eyeballed: relative luminance per WCAG 2.x over `#898781` against `#fcfcfb` and `#f0efec`. Found while auditing a *new* colour pairing rather than by any check, which is the same way L-004 was found and the reason both are still open. | 2026-08-10 | 2026-09-21 | `open` — the two new chronology elements added under C3 use `--c-text-soft` instead (6.90:1 light, 6.57:1 dark) so nothing new was added to the pile, but the existing uses are untouched. **The tripwire this wants: contrast asserted in the token layer**, the way `stylelint-declaration-strict-value` already asserts that raw colours never leave `tokens.css`. A palette with a rule about where colours live and no rule about whether they can be read is half a system |
| L-035 | Medium | tooling | **The year check is structurally blind to day-level dates, and the chronology carried four invented ones.** `YEAR` is `/\b(?:1\d{3}\|20\d{2})\b/` — it reads four-digit years and nothing finer, so "7 May 2015", "13 July 2016" and "23 June 2016" all passed while appearing **zero** times in the handbook, which gives only "Coalition government from 2010 to 2015", "the 2016 EU membership referendum" and no days at all. A fourth, "24 July 2019", was caught only because 2019 is itself absent from the book. The invented precision is the dangerous part: a learner drilled on "23 June 2016" has been taught a fact the exam cannot ask and the source does not hold. | 2026-08-10 | 2026-09-21 | `open` — the four are removed in the C3 rewrite and the corpus scanner (`scanCorpus`) now covers `timeline.ts`, which closes the *coverage* gap that let the chronology go unscanned for its whole life. The *precision* gap remains: nothing detects a day or month the handbook does not give. Wants a check that flags any `\d{1,2} <Month> \d{4}` string not present verbatim in the handbook |

| L-036 | **High** | measurement | **The numeric-tell measurement was ranking days of the month.** `readNumber` takes the first integer in an option, so "8 May 1945" reads as **8**. A form offering `8 May | 15 August | 11 November | 6 June` was ranked 8/15/11/6 and scored as "the correct answer is a middle value" — but the thing being ranked is a day number, which is not the answer's magnitude and which no reader perceives as a size. 23 of the deck's 405 measured numeric forms were calendar dates, and they were the largest single block in the as-written residual that `effectiveNumericMiddleRankRate` exists to track. **This inflated the headline figure that R-7 and the whole readiness model are gated on.** Date *ranges* are a different case and are correctly measured: "1853-1856" reads as 1853, which genuinely is the magnitude being compared. | 2026-08-10 | 2026-08-24 | `fixed-unverified` — calendar-date sets excluded from `numericMiddleRankRate` and `effectiveNumericMiddleRankRate`. **This needs a fresh session more than any other row here, and the reason is uncomfortable: it was found while the numeric ratchet was failing, and fixing it is what let the build go green.** A measurement change that unblocks its author's own commit deserves the most scrutiny available. Both figures are recorded so the change can be judged rather than taken on trust — with the dates included the deck measured **0.5305** against a 0.53 ceiling; with them excluded, **0.5249**, on 382 forms instead of 405. The pre-audit deck measured 0.5249 on the same definition, so the audit is neutral on this metric rather than an improvement |

| L-037 | **High** | process | **The option audit re-flagged two facts the owner had personally settled, and a crude verification would have retired them.** The audit referred 36 answers to the owner as possibly wrong. An independent word-presence check over the handbook found exactly two whose answer shares no distinctive word with the source — `f098` (1651, Worcester) and `f391` (1985, the FGM Act) — which reads as "strong retire". Both are recorded in L-031 as **settled**: enumerated D-031 deltas the owner confirmed against his own 2026 edition, where the extracted proxy text is known to be wrong. Retiring on that signal would have deleted two correct facts *because the proxy disagrees with the authority*, which is R3 running backwards. **The audit agents were never told the D-031 delta list exists.** | 2026-08-10 | 2026-08-24 | `open` — three facts were retired from the 36 (f215, f216, f219, the citizenship-ceremony trio, each re-grepped by hand first); the other 33 are a review list, not an action. The tripwire this wants: the D-031 settled-deltas list should live in the deck as data, and any pass that proposes changing a fact's answer must be given it. Today it exists only as prose inside a ledger row |

## Notes

**L-002 and L-003 are the reason the readiness model excludes multiple-choice data until they close
(D-004/D-013).** They are ratcheted rather than fixed because fixing them is a content programme, not
a bug fix, and a red build on day one teaches everyone to ignore red builds.

**L-006 and L-007 are both cheap to fix and neither needs an index remap** — one is a reworded form,
the other is two `mcqOnly` flags. What they cost is recall-usable forms, which are already scarce
(L-008), so they should be resolved in the same pass that adds forms rather than on their own.

### Postmortem — screen state derived from the event log (L-019, L-021)

**Trigger:** the same bug fixed twice, which the BRIEF pre-declares as a postmortem trigger.

1. **What happened.** Twice, something the reader was looking at was computed from the review
   log. Answering appends to that log, so the thing under their eyes changed at the moment
   they acted on it. First the whole card swapped (L-019); then the four options re-ordered
   (L-021).
2. **Why it recurred.** The first fix treated the symptom — it held the *card* still — rather
   than the class. The option shuffle was seeded from a live count three lines away and nobody
   looked, because the card itself had stopped moving and the bug appeared solved.
3. **Why the tests missed it.** 137 tests passed. The domain layer is pure and correct in both
   cases; the defect only exists where React re-renders against changed inputs. Component
   tests existed by then, but the first version of the new regression test happened to open a
   *chapter* drill — the one section whose count does not move when you answer — and passed
   against broken code.
4. **The tripwire.** Regression tests now enumerate the sections rather than sampling one, and
   both fixes are verified by reverting them and watching the tests fail. Added as RULES R-11
   so the class is named, not just the two instances.
5. **The lesson worth keeping.** A test that has never failed is not yet known to test
   anything. Both of these were only trustworthy after being run against the broken code.

**A note on how L-006 was found and mis-stated.** It surfaced from a duplicate-question scan during
Phase 0 and was written up as "the same fact, twice". The first run of `npm run deck:report`
contradicted it — zero duplicate canonical questions — and the facts turned out to be genuinely
different with one shared sentence between them. Two things follow. The check that would have caught
it existed in neither direction, so `sharedFormsAcrossFacts` now exists. And a finding written from a
scan but never re-derived from the code is a finding that can be wrong for weeks; the report script is
the cheap way to keep re-deriving them.
